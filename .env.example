GROQ_API_KEY=
D_MAX_MS=1500
GUARD_MS=300
READY_TIMEOUT_MS=4000
PREP_MS=200
TICK_MS=500
DB_PATH=diverttrack.db
SPEED_KM_PER_MIN=1.0
LOAD_WEIGHT=10
SATURATION_LIMIT=0.9
POLICY=auto
# Demo pacing. SPEED_KM_PER_MIN stays a real clinical figure because
# eta_minutes() feeds the transport-time acceptance check; wall-clock is
# compressed instead.
SIM_TIME_SCALE=45
MIN_TRAVEL_MS=12000
# How long the least critical patient occupies a bed; scaled up by acuity.
# 0 disables automatic discharge.
TREATMENT_MS=8000
