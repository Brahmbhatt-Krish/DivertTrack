[pytest]
pythonpath = .
testpaths = tests
# K: is a mapped drive whose rename semantics reject pytest's cache-dir
# swap (WinError 5); the cache plugin is pure bookkeeping we don't need.
addopts = -p no:cacheprovider
